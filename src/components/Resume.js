import React from 'react';
import ResumeSection from './ResumeSection';

function Resume() {
  return (
    <div>
      <ResumeSection />
      {/* Placeholder h1 removed, ResumeSection now provides the content */}
    </div>
  );
}

export default Resume;
